version: '3.8'

services:
  kfs1-build:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    volumes:
      - .:/workspace
  kfs1-run:
    image: kfs1-kernel:latest
    volumes:
      - .:/workspace
    command: bash

volumes:
  build-artifacts:

